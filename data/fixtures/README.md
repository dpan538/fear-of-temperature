# Synthetic fixtures

`synthetic_passages.jsonl` is invented test material for software verification. It is not
historical evidence, a collected corpus, a human-annotated gold set, or a claim about any
institution, publisher, community, or population. The `fixture_*` fields are expected labels
used to exercise denominators, missingness, provenance, quotation scope, and model interfaces.
