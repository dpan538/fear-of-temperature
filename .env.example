# Copy to .env for local use. Values below are non-secret project defaults.
FOT_DATA_DIR=data
FOT_OUTPUT_DIR=outputs
FOT_MODEL_CACHE=.cache/huggingface
FOT_DEVICE=auto
FOT_RANDOM_SEED=20260916
FOT_DATABASE_URL=
HF_HOME=.cache/huggingface
PYTORCH_ENABLE_MPS_FALLBACK=1
TOKENIZERS_PARALLELISM=false
LOKY_MAX_CPU_COUNT=8
